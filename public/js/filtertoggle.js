$('.filter-button').click((e)=>{
    // if($(e.currentTarget).html('Show Filters')){
    //     $(e.currentTarget).html('Hide Filters');
    // }if($(e.currentTarget).html('Hide Filters')){
    //     $(e.currentTarget).html('Show Filters');
    // }
    $('.filter-form').toggle();
})